{% extends 'content_base.html.twig' %}

{% block content_body %}

    <h1> Launch a new Comment!</h1>

    {{  form_start(commentForm) }}
        {{ form_widget(commentForm) }}

        <button type='submit' class='btn btn-primary'> Create </button> 

    {{  form_end(commentForm) }}



{% endblock %}